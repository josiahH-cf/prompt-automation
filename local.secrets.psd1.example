@{
  # Todoist REST API token for creating tasks via :ntsk
  # Never commit real secrets. Copy this file to 'local.secrets.psd1' and fill in a real token.
  # Precedence: the script uses the TODOIST_API_TOKEN environment variable first, then this file.
  TODOIST_API_TOKEN = '<REDACTED_REPLACE_ME>'
}
